now for update (map from the slides)

takes a message and a model and returns a model

``` elm
update : EdocMsg -> EdocModel -> EdocModel
update msg model =
    model

```

... great, compiles, but doesn't do anything
soon we'll add some excitement
