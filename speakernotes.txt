great, let's make the click do something!

we'll need to add html events
```
import Html.Events exposing (onClick)
```

and update the view to send that message when the title is clicked
```
    h1 [ onClick TitleClicked ] [ text model ]
```

just like js
