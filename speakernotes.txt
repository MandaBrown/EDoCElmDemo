wait, still doesn't compile - need to fix type annotation

```
The type annotation for `main` says it is a:

    Html msg

But the definition (shown above) is a:

    Program Never String EdocMsg
```
