what if we wanted to set a level of excitement?

by say, listening for keypresses?

keyboard package
http://package.elm-lang.org/packages/elm-lang/keyboard/1.0.1/Keyboard

> This library lets you listen to global keyboard events.

subs hmmm... anyway, let's install it

```bash
elm package install elm-lang/keyboard
```
